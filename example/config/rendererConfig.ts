import { sRGBEncoding } from "three";
import { IWebglRendererConfig } from "@shd-developer/interactive-map";

export const config = {
    outputEncoding: sRGBEncoding
} as IWebglRendererConfig;

export default config;